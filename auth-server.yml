server:
  port: 6000
  servlet:
    context-path: /auth
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
info:
  app:
    name: Auth server
    description: App de clientes
    version: 0.0.1-SNAPSHOT